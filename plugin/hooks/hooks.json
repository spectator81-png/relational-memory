{
  "description": "Relational Memory — loads relationship context at session start, auto-saves signals at stop",
  "hooks": {
    "SessionStart": [
      {
        "type": "command",
        "command": "python \"${CLAUDE_PLUGIN_ROOT}/scripts/session_start.py\"",
        "timeout": 15
      }
    ],
    "Stop": [
      {
        "type": "prompt",
        "prompt": "Read and follow the auto-save instructions: use the Read tool to read ${CLAUDE_PLUGIN_ROOT}/prompts/auto_save.md, then execute every step described there.",
        "timeout": 120
      }
    ]
  }
}
