{
  "description": "Relational Memory — loads relationship context at session start",
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "python \"${CLAUDE_PLUGIN_ROOT}/scripts/session_start.py\"",
            "timeout": 15
          }
        ]
      }
    ]
  }
}
